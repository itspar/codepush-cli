export const CodePushConfigConstants = {
    SCRIPT_DIRECTORY: "patch-scripts",
    CREATE_PATCH_SCRIPT: "create-patch.sh",
    APPLY_PATCH_SCRIPT: "apply-patch.sh",
}

export enum CompressionType {
    DEFLATE = 'deflate',
    BROTLI = 'brotli',
}